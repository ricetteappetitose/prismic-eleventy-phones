---
layout: default
meta_title: All phones
meta_description: All updated smartphones
body_class: blog
title: All phones
---

<main id="main" class="main inner flex flex-1 flex-col py-10 lg:py-20 focus:outline-none" tabindex="-1">

    <section id="container-centre" class="column centre flex-1" aria-label="Blogs">

        <div class="posts flex flex-wrap">

            {% for phone in phones %}

                <article class="article-card mb-8 mx-4 w-full md:w-1/3 lg:w-1/5 bg-white overflow-hidden rounded-lg shadow-lg">

                    {# featured post image & category #}
                    {% if phone.data.image %}

                        <div class="featured-image-link block relative overflow-hidden">

                            <figure>

                                {# the 'data-lazy' attribute will lazy load your images in #}
                                {# the js uses the new Intersection Observer API, see ../modules/lazyload #}
                                {# it's not fully supported in some browsers, so proceed with caution! #}
                                {# the srcset attribute would be a nice addtion here as well...in a future update maybe :) #}
                                <img class="h-px" data-lazy="{{ phone.data.image.url }}" class="block" alt="">

                            </figure>

                        </div>

                    {% endif %}

                    <div class="p-6">

                        <h2 class="text-2xl mb-4"><div class="block text-purple-900 hover:text-pink-500">{{ phone.data.brand }} {{ phone.data.name[0].text }}</div></h2>

                        <div class="text-sm text-gray-600 md:flex mb-2">

                            <p class="mb-2">Updated:
                              <time datetime="{{ phone.data.os_versions[0].updated }}">
                                  {{ phone.data.os_versions[0].updated | dateDisplay }}
                              </time>
                            </p>

                            <p class="author">Released: {{ phone.data.year }}</p>

                        </div>

                    </div>

                </article>

            {% endfor %}

        </div>

    </section>

</main>
